---
title: "Late Tokens"
description: "Use late tokens to extend assignment deadlines by 24 hours without penalty"
icon: "clock"
---

# Late Tokens

Pawtograder supports late submission policies based on "late tokens." Instructors determine how many late tokens are available to each student, and how many tokens can be used for each late submission. Each token extends the deadline by 24 hours, with no penalty.

<Warning>
By default, late tokens must be applied **before** the deadline. If multiple late tokens are permitted, you can apply them all at once or one at a time, but you must always apply them before the updated deadline. Some instructors may configure assignments to allow automatic late token application — see below.
</Warning>

## Applying Late Tokens

On each assignment page where late tokens can be applied, you'll see an **Extend Due Date** button next to your due date.

<Frame>
  <img src="/images/students/assignments/late-tokens-1754513689169.png" alt="Assignment page showing Extend Due Date button" />
</Frame>

A confirmation dialog explains the course's policy (configured by your instructor).

<Frame>
  <img src="/images/students/assignments/late-tokens-1754590579057.png" alt="Late token confirmation dialog with course policy" />
</Frame>

After applying the token, you'll see the new due date.

<Frame>
  <img src="/images/students/assignments/late-tokens-1754590617225.png" alt="Updated due date after applying late token" />
</Frame>

To use multiple late tokens, click the **Extend Due Date** button again.

## Automatic Late Token Application

Some instructors configure assignments to allow late tokens to be applied **automatically** when you submit after the deadline. When this is enabled:

- You do **not** need to apply a token before the deadline — just push your code.
- When you submit after the deadline, Pawtograder automatically deducts the required number of tokens and extends your deadline to cover the submission time.
- If you don't have enough tokens remaining, your submission will be rejected with an error message explaining how many tokens are needed.
- The assignment page will show a notice: *"Deadline passed: submitting will auto-apply a late token if you have one remaining."*

When auto-apply is **not** enabled (the default), the assignment page shows: *"Tokens must be applied before the due date."* In that case, you must use the **Extend Due Date** button before the deadline passes.

<Note>
Whether auto-apply is enabled is an instructor-configured setting per assignment. Check the assignment page for the notice that describes the policy for that specific assignment.
</Note>

## Gifted Late Tokens

Instructors can gift additional late tokens to individual students beyond the class-wide allocation, on a case-by-case basis. Gifted tokens:

- Are automatically added to your available token balance
- Work exactly like your original tokens—each extends a deadline by 24 hours and must be applied before your current due date
- Display in your remaining token count (which always reflects both your initial tokens and any gifted tokens)
- Can be used even if you've exhausted your initial allocation
- Are tracked separately in the system (they appear as negative values internally) for instructor visibility

For example, if you've used all 4 of your initial tokens but your instructor gifts you 1 additional token, you'll see 1 remaining token available to use.

## Late Tokens and Group Assignments

The semantics of late tokens for group assignments are intended to be as straightforward and fair as possible:

- Any student in the group can apply a late token as long as they have one available (and the maximum number of tokens hasn't yet been applied to the assignment). It does not matter if a groupmate is out of tokens.
- All group members will have a token deducted from their total.
- All group members will see the same due date.
