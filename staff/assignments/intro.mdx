---
title: "Assignments Overview"
description: "Programming assignments with GitHub repos, autograders, handgrading, and group support"
icon: "code"
---

# Assignments Overview

Most Pawtograder assignments are backed by a **handout** repository (starter
code) and, optionally, a **grader** repository (instructor tests and the
`pawtograder.yml` config). When you release an assignment, Pawtograder
creates a private student repository from the handout for every student
(or group). The autograder runs on every push and reports results back
to Pawtograder, where you can handgrade against rubrics, release results,
and respond to regrade requests. That is the default configuration and
what the student documentation describes.

It is not the only one. An assignment can also be configured with no
autograder, with student forks of a visible handout, with a fork of the
student's own repository from a prior assignment, with pull-request
submission, with direct file upload, or with no artifact at all. Each of
those changes what a student sees and what counts as a submission — see
[Configuration → Student repositories](/staff/assignments/configuration#student-repositories)
and [Configuration → Submission mode](/staff/assignments/configuration#submission-mode).

## First-time setup checklist

<Steps>
<Step title="Create the assignment">
From **Manage → Assignments**, click **New Assignment**. Pick a title and
slug, then fill in:

- **Release date** and **Due date** (in the course time zone).
- **Points possible** — the maximum the rubric can sum to.
- **Max late tokens** — `0` by default, which means no late tokens on
  this assignment.
- **Submission type** — *Individual Submissions Only*, *Group Submissions
  Only*, or *Individual or Group Submissions*. Required, with the
  individual option preselected.
- **Repository configuration** — how student repositories are created.
  Defaults to a staff-only template repo that each student gets a fresh
  private copy of. The other four options change what a student sees on
  the assignment page.
- **Enable autograder (GitHub Actions)** — checked by default. Uncheck it
  for a repo-only assignment you grade by hand.
- **Submission mode** — `push` by default. Pull-request mode is only
  offered for the two fork-based repository configurations.
- **Show Leaderboard** if you want a public ranking (in **Advanced
  settings**, which is collapsed).

Set the repository configuration and submission mode before the release
date; changing them once students have repositories is not a clean
operation. See [Configuration](/staff/assignments/configuration) for the
full field reference.
</Step>

<Step title="Set up the grader repo">
The grader repo holds your reference solution, instructor tests, and
`pawtograder.yml`. The handout repo holds the starter that students see.
Write the autograder config in the grader repo; see
[Configuring Autograders](/staff/assignments/autograding).
</Step>

<Step title="Define the rubric">
Build the **Grading Rubric** in the rubric editor. The rubric defines
the parts, criteria, and checks that graders apply during handgrading.
See [Rubrics](/staff/assignments/rubrics).
</Step>

<Step title="Release and monitor">
Releasing the assignment creates student repositories and notifies
students. Use the [Assignment Dashboard](/staff/assignments/assignment-dashboard)
for overall progress, the [Grading Progress Dashboard](/staff/assignments/grading-status)
tab under **Grading Assignments** for handgrading progress, and [Test Insights](/staff/assignments/test-insights)
for systemic autograder failures.
</Step>
</Steps>

## What's on each assignment

Each assignment has its own management area inside **Manage → Assignments
→ [Assignment]**, organized into pages that map to the docs in this
section:

| Page | Docs |
|---|---|
| Edit Assignment | [Configuration](/staff/assignments/configuration) |
| Repository Status (and permissions audit) | [Repository Permissions](/staff/assignments/repository-permissions), [Repository Analytics](/staff/assignments/repository-analytics) |
| Manage Groups | [Groups](/staff/assignments/groups) |
| Manage Due Date Exceptions | [Extensions](/staff/assignments/extensions) |
| Configure Rubric | [Rubrics](/staff/assignments/rubrics) |
| Configure Autograder, Test Insights | [Configuring Autograders](/staff/assignments/autograding), [Test Insights](/staff/assignments/test-insights) |
| Grading Assignments, including its **Grading Progress Dashboard** tab | [Assigning Grading Tasks](/staff/assignments/grading-assignments), [Grading Status](/staff/assignments/grading-status) |
| Assignment Home, **Dashboard** tab (handgrading and bulk actions) | [Handgrading](/staff/assignments/handgrading), [Bulk Actions](/staff/assignments/bulk-actions) |
| Security Audit | [Security Dashboard](/staff/assignments/security-dashboard) |

The leaderboard is not a management page. It is the **Show Leaderboard**
checkbox under **Advanced settings** on the Edit Assignment form; see
[Leaderboard](/staff/assignments/leaderboard).

## GitHub permissions

Pawtograder creates two GitHub teams for each class — a **staff** team
and a **students** team. Staff are granted access to every assignment
repository; students are granted access to only their own. The students
team can be used by instructors to share additional read-only
repositories (in-class exercises, code samples) with the whole class.

Pawtograder reconciles repository collaborators against current group
membership whenever you click **Fix Repository Permissions** on an
assignment's Repositories page. Run the audit after roster changes,
group reshuffles, or when a student reports a missing repo. Details and
the full audit flow are in
[Repository Permissions](/staff/assignments/repository-permissions).

## Syncing handout updates

After an assignment is released you can push updates from the handout
repo to every student repo while preserving each student's work. The
sync detects merge conflicts, retries transient failures, and lets you
target a subset of students. Trigger it from the assignment's
**Repositories** page.
