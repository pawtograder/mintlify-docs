---
title: "Course Settings"
description: "Configure course-wide settings including time zones, late tokens, and grading policies"
icon: "gear"
---

# Course Settings

Course settings allow instructors to configure global preferences that apply across all course features.

## Time Zone Configuration

Pawtograder supports configurable time zones for displaying dates and times throughout the platform. This ensures that all deadlines, timestamps, and schedules are shown in the appropriate time zone for your course.

### Setting Your Course Time Zone

1. Navigate to **Course Settings** in the course navigation menu
2. Find the **Time Zone** setting
3. Select your preferred time zone from the dropdown menu
4. Click **Save** to apply the changes

Once set, dates and times throughout Pawtograder default to this time zone for all users in the course. Individual viewers may switch their display to their browser's local time zone via the time-zone toggle in the user menu (Pawtograder prompts on first visit when the two differ). The preference is stored per-browser in `localStorage`; it does not change the course's configured time zone or affect other users.

### Default Time Zone

If no time zone is configured, Pawtograder defaults to **America/New_York** (Eastern Time).

### Where Time Zones Apply

The configured time zone affects:

- **Assignment due dates**: Deadlines shown to students and staff
- **Survey deadlines**: When surveys close for responses
- **Poll deactivation times**: Automatic poll closing times
- **Office hours schedules**: Queue availability times
- **Gradebook timestamps**: When grades were entered or updated
- **Discussion board posts**: Post and reply timestamps
- **Self-review deadlines**: When self-reviews are due
- **Submission timestamps**: When students submit work
- **Export files**: Timestamps in the configured time zone

### Time Zone Display

When a time zone is configured:

- Dates and times default to the course's configured time zone for all users; each viewer may opt to display them in their browser's local time zone instead (per-browser preference, stored in `localStorage`)
- Countdown timers automatically calculate based on the configured time zone
- Date pickers use the configured time zone for deadline selection
- Export files include timestamps in the configured time zone

### Admin View Default Time Zone

For system administrators viewing courses across multiple time zones, a default time zone can be configured for the admin view to ensure consistent time display when managing multiple courses.

<Note>
The time zone setting is course-wide and applies to all students and staff in the course. Individual users cannot override this setting.
</Note>

<Tip>
Set your time zone to match your institution's location or the primary location where your course is taught. This ensures that due dates and times are intuitive for your students.
</Tip>

## Late Token Policy

Configure the class-wide late token policy that determines how many late tokens students receive and how they can be used for assignment extensions.

### Configuring Late Tokens

1. Navigate to **Course Settings**
2. Find the **Late Tokens** section
3. Set the number of late tokens available to each student
4. Configure how many tokens can be used per assignment (set per-assignment)
5. Define the extension period for each token (typically 24 hours)

See [Extensions](/staff/assignments/extensions) for more details on late token configuration, and the [Late Tokens and Extensions](/students/assignments/late-tokens-and-extensions) documentation for how students use late tokens.

## Grading Conflicts

Course staff can declare conflicts of interest to ensure they don't grade assignments where conflicts exist.

Graders can self-input their conflicts under **Course Settings** → **Grading Conflicts**, and instructors can review and enter conflicts for all graders in the class.

See [Grading Assignments](/staff/assignments/grading-assignments) for more details on managing grading conflicts.

## Course Display Settings

Additional course-wide settings may include:

- Course name and description
- Enrollment synchronization preferences
- GitHub organization settings
- Email notification preferences

## Deployment branding (self-hosted installs)

Self-hosted Pawtograder deployments can re-brand the application — service name, tagline, logos, favicon, and accent color — without rebuilding the web image. Branding is configured via environment variables injected by the Helm chart at deploy time and read server-side on every request.

| Variable | Description |
|---|---|
| `BRAND_NAME` | Product name shown in page titles, headings, and wordmarks. |
| `BRAND_DESCRIPTION` | `<meta name="description">` / default site description. |
| `BRAND_TAGLINE` | Short line shown under the wordmark on auth screens. |
| `BRAND_LOGO_LIGHT` | Logo for light mode — bundled path (e.g. `/branding/logo-light.png`) or absolute `https://` URL. |
| `BRAND_LOGO_DARK` | Logo for dark mode (same rules as `BRAND_LOGO_LIGHT`). |
| `BRAND_FAVICON` | Browser-tab icon — bundled path or absolute URL. |
| `BRAND_COLOR_PALETTE` | Chakra accent palette: `gray`, `red`, `orange`, `yellow`, `green`, `teal`, `blue`, `cyan`, `purple`, or `pink`. |
| `BRAND_SSO_PROVIDERS` | JSON array of sign-in SSO buttons (see below). |

Any variable left unset keeps the built-in Pawtograder default. Configure these under `web.branding` in your Helm values file:

```yaml
web:
  branding:
    name: "MyGrader"
    tagline: "Grading for your institution"
    logoLight: "/branding/logo-light.png"
    logoDark: "/branding/logo-dark.png"
    favicon: "/branding/favicon.png"
    colorPalette: "teal"
```

### Custom logos and favicon

Custom logos and the favicon can be provided two ways:

- **Absolute `https://` URL** — logos render with a plain `<img>` tag; no `next.config` image-host allow-listing is needed.
- **Baked into the image (recommended)** — drop files into `public/branding/` before `docker build`. Everything under `public/` is served at `/branding/*` inside the image. Point the values at those paths (e.g. `/branding/logo-light.png`).

### Configuring SSO buttons

The sign-in page renders one button per provider in `web.branding.ssoProviders`. Leaving it empty keeps the default Microsoft (Northeastern Login) button. Each provider must also be enabled in GoTrue.

```yaml
web:
  branding:
    ssoProviders:
      - provider: google
        label: "Continue with Google"
        icon: google
      - provider: azure
        label: "Continue with Microsoft"
        icon: microsoft
        scopes: "email User.Read"
```

Supported `provider` values: `apple`, `azure`, `bitbucket`, `discord`, `facebook`, `figma`, `github`, `gitlab`, `google`, `kakao`, `keycloak`, `linkedin_oidc`, `notion`, `slack_oidc`, `spotify`, `twitch`, `workos`, `zoom`. To show no SSO buttons (email-only sign-in), set `BRAND_SSO_PROVIDERS: "[]"` via `web.extraEnv`.

See the chart README "Deployment skinning / branding" and "Single sign-on (SSO)" sections for full configuration details.
