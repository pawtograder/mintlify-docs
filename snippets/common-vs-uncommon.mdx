{/* Reusable staff callout. Files under /snippets are not rendered as pages.
    Import it with:
      import CommonVsUncommon from "/snippets/common-vs-uncommon.mdx";
    then place <CommonVsUncommon /> where the callout belongs. */}

<Note>
**Common vs. uncommon.** Every setting on this page is real and supported. The ones marked
*default* are what a new assignment gets and what the
[student guide](/students/assignments/create-submission) describes. Anything marked *uncommon*
will surprise students, because Pawtograder's interface does not explain your reasoning for
you — if you turn one on, say so in your own course materials.
</Note>
